# ==========================================
# NETWORK CONFIGURATION
# ==========================================

# Cloud/External server IP or domain for accessing the API
# For Kubernetes external access, use your cluster's external IP or domain
# Examples:
#   - 192.168.1.5 (local network)
#   - your-domain.com (domain name)
#   - 35.123.45.67 (cloud provider IP)
LOCAL_NETWORK_IP=

# Cloud Gateway port number (Kubernetes NodePort or LoadBalancer port)
# For external access, this is typically a NodePort (30000-32767) or LoadBalancer port
GATEWAY_PORT=

# ==========================================
# SERVICE ENDPOINTS (API Paths)
# ==========================================

# Authentication service path
AUTH_SERVICE_URL=/api/v1/auth

# Notes service path
NOTES_SERVICE_URL=/api/v1/notes

# Passwords service path
PASSWORD_SERVICE_URL=/api/v1/passwords

# OTP service path
OTP_SERVICE_URL=/api/v1/otp

# ==========================================
# HTTP CONFIGURATION
# ==========================================

# Request timeout in seconds (how long to wait for a response)
# For remote Kubernetes servers with higher latency, use higher values
# Recommended values:
#   - 15-30: Fast local network
#   - 45-60: Cloud/external servers with moderate latency
#   - 60+: Slow networks or remote servers
REQUEST_TIMEOUT=60

# Maximum number of automatic retries for failed requests
# Helps handle transient network issues
MAX_RETRIES=3

# Initial retry delay in milliseconds (exponentially increases after each retry)
# Example: 500ms -> 1000ms -> 2000ms (with exponential backoff)
INITIAL_RETRY_DELAY=500

# ==========================================
# DEBUG CONFIGURATION
# ==========================================

# Enable debug mode (shows detailed logs)
# Values: true or false
DEBUG_MODE=true
